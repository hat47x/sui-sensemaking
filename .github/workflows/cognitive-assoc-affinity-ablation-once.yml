name: cognitive-assoc-affinity-ablation-once

on:
  push:
    branches:
      - research/cognitive-assoc-01-learned-sparse-gate-20260919
    paths:
      - .github/workflows/cognitive-assoc-affinity-ablation-once.yml

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Verify T5 to T6 preregistered gates
        working-directory: 01_Plans/dogfood
        run: |
          python -m unittest -v \
            test_build_cognitive_assoc_learned_sparse_gate.py \
            test_build_cognitive_assoc_affinity_ablation_plan.py

      - name: Verify fixed v0 T6 artifacts do not exist
        run: |
          set -euo pipefail
          for file in \
            01_Plans/dogfood/cognitive-assoc-benchmark-v0-affinity-ablation-source.json \
            01_Plans/dogfood/cognitive-assoc-benchmark-v0-affinity-ablation-plan.json \
            01_Plans/dogfood/cognitive-assoc-benchmark-v0-affinity-ablation-summary.json
          do
            test ! -e "$file"
          done
          echo "FIXED_V0_T6_EXECUTION: NOT_EXECUTED"

      - name: Documentation and diff checks
        run: |
          set -euo pipefail
          git diff --check
          set +e
          python 01_Plans/docs_check.py > "${RUNNER_TEMP}/docs-check.out" 2>&1
          status=$?
          set -e
          if [ "$status" -ne 0 ]; then
            cat "${RUNNER_TEMP}/docs-check.out"
            python - "${RUNNER_TEMP}/docs-check.out" <<'PY'
          import pathlib
          import sys

          lines = [
              line.strip()
              for line in pathlib.Path(sys.argv[1]).read_text(encoding="utf-8").splitlines()
              if line.strip().startswith("- DC-")
          ]
          expected = "issue-ARCH-TEI-MIGRATION-01-tei-reference-adoption-and-development-load-validation.md"
          assert len(lines) == 2, lines
          assert all(expected in line and "Planned" in line for line in lines), lines
          print("DOCS_CHECK: only two pre-existing main errors remain")
          PY
          fi
